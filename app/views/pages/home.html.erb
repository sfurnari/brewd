<%= form_tag(search_path, :method => "get") do %>
    <div class="search-input">
        <%= search_field_tag :search, params[:search], placeholder: "Search for Cafes, Roasters or Users" %>
        <div class="search-btn">
            <%= button_tag "Search" %>
        </div>
    </div>
<% end %>