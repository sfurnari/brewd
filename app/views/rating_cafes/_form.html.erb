<%=  simple_form_for([@cafe, @cafe.rating_cafes.build]) do |f| %>
    Rate
    <%= f.number_field :rating, {min: 0, max: 5} %> / 5
    <br>
    <br>
    <%= f.input :review %>
    <%= f.button :submit, "Submit" %>
<% end %>