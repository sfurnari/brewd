<div class="index-header">
    <h2>All Cafes</h2>
</div>
<ul>
    <% @cafes.each do |c|%>
        <li>
            <%= link_to c.name, cafe_path(c) %> - <%= c.address %>
        </li>
        <br>
    <% end%>
</ul>